# ZIRH-3 requirements - the traceability chain from the first commit
# (PROGRAM.md H49). scripts/trace_check.py gates CI on it: every
# requirement names its tests and its evidence, no orphans either way.
requirements:
  - id: Z3-R1
    text: A single upset in any one TMR replica of any imported block
      shall never reach a voted output, in any cycle.
    tests:
      - formal/f_ring.sv
    evidence: BMC + k-induction at N=4/8/32 in the formal workflow,
      inherited proof carried live, not cited

  - id: Z3-R2
    text: The memory word shall correct any single-bit storage error,
      detect any double-bit error, and detect ANY read from a wrong
      row as uncorrectable (address-in-ECC).
    tests:
      - formal/f_ecc.sv
      - formal/f_amask.sv
      - test/test_sram39.py
    evidence: SECDED exhaustive proof + even-weight fold proof +
      the 5-slice suite with double-bit and background-scrub cases

  - id: Z3-R3
    text: The memory shall carry a background scrubber that walks the
      whole address space at a fixed rate, itself TMR with a
      safe-state trap, so rare-read words cannot accumulate errors.
    tests:
      - test/test_sram39.py
    evidence: scrub beat and scrub-fix cases in the suite;
      SCRUB_DIV_LOG2 parameter proven at 3

  - id: Z3-R4
    text: A word built from five physical macro slices shall reduce
      any single-macro multi-bit upset to at most one logical bit
      per word.
    tests:
      - test/test_sram39.py
    evidence: the corrupted-merge case; the generate-loop slice
      structure (one definition, five instances)

  - id: Z3-R5
    text: The boot controller shall verify the STORED image by
      read-back CRC before running it, refuse corrupt or oversize or
      wrong-magic images, survive interrupted loads, and revert a
      bank the watchdog fails toward the other bank and then the
      immutable golden copy.
    tests:
      - test/test_boot.py
      - test/sv/zirh_boot_scenarios_tb.sv
    evidence: cocotb contract suite + eight SV stories including
      storage_lie, where only a lying memory can prove the read-back
      claim

  - id: Z3-R6
    text: The QSPI controller shall stream boot images from external
      MRAM in x1 and x4 modes with backpressure expressed as SCK
      freeze, never dropping or duplicating a byte.
    tests:
      - test/test_qspi.py
    evidence: mode-0 timing, QOR dummy cycles and freeze cases in
      the suite; embedded assertions live in simulation

  - id: Z3-R7
    text: The debug port shall be lockable for flight at POR, deny
      all access when locked, and trap its own TMR mismatch to the
      LOCKED state.
    tests:
      - test/test_dbg_gate.py
      - formal/f_dbg.sv
    evidence: lock ladder proof + suite

  - id: Z3-R8
    text: A clock-loss event on the main clock shall be detected and
      latched by an observer running on an independent oscillator,
      with a two-flop synchronized handoff.
    tests:
      - test/test_clkobs.py
    evidence: main-clock-kill case in the suite; the observer
      deliberately carries no main-clock assertions

  - id: Z3-R9
    text: The march/pattern BIST engine shall drive the macro word
      through checkerboard and march elements in static and dynamic
      modes and report per-address failures raw.
    tests:
      - test/test_bist.py
    evidence: pattern engine suite; the A6 bare-macro experiment
      builds on this engine

  - id: Z3-R10
    text: Every TMR structure in the library shall survive synthesis
      with its replicas intact, proven by counting on the optimized
      netlist, with expectations MEASURED and updated only alongside
      the RTL.
    tests:
      - scripts/check_tmr.sh
    evidence: per-block synthesis-integrity run in CI; measured
      numbers recorded at import
  - id: Z3-R11
    text: The memory/boot subsystem shall integrate the loader, the
      sliced SECDED bank, the QSPI image source, the clock-loss
      observer and the debug gate into one harness-independent unit
      that comes out of reset with the loader in charge, the debug
      port locked, and no spurious TMR error - every block keeping its
      replicas through synthesis.
    tests:
      - test/tb_memsys.v
      - scripts/check_tmr.sh
    evidence: zirh3_memsys elaboration smoke (golden strap -> no bank,
      POR debug lock, clean err) + the integration synthesis-integrity
      entry; per-block merge guards remain authoritative
  - id: Z3-R12
    text: The die shall condition its own system reset from the raw pad
      reset plus a brown-out signal (hold through a settle window,
      release synchronized, re-arm on power dropout) and generate its
      own independent oscillator for the clock-loss observer, so the
      standalone die needs no external system reset or observer clock.
    tests:
      - test/tb_por_ro.v
      - test/tb_die.v
      - scripts/check_tmr.sh
    evidence: POR sequence smoke (settle hold, clean release, RO runs,
      brown-out re-arm) + die-level smoke (self-conditioned reset
      brings up the subsystem, loader in charge, debug locked); the
      POR/RO source carries no TMR by design and its count is guarded
  - id: Z3-R13
    text: The SRAM DUT experiment shall drive five BARE macros (no
      SECDED, no scrubber, no voting) through pattern scans and expose
      every mismatch as a raw (address, per-macro bit-map) record, so a
      beam campaign reads the unprotected macro's SEU/MBU cross-section
      directly and the ground analysis can separate single-bit from
      multi-bit upsets within one physical macro.
    tests:
      - test/tb_sram_dut.v
      - scripts/check_tmr.sh
    evidence: clean-array baseline smoke (zero failures with no
      radiation - the floor a campaign subtracts); the pattern engine
      is TMR while the macros under test are deliberately not
  - id: Z3-R14
    text: The die shall carry a JTAG debug port - an IEEE 1149.1 TAP
      speaking the RISC-V external-debug transport (IDCODE, DTMCS, DMI)
      and a Debug Module producing halt/reset/system-bus-access
      requests - whose every effect reaches the core ONLY through the
      flight-locked isolation gate: inert in flight, live on the bench.
    tests:
      - test/tb_jtag.v
      - scripts/check_tmr.sh
    evidence: JTAG smoke - IDCODE shifts out correct, a dmcontrol
      haltreq is held inert by the locked gate (the DM asks, the gate
      refuses) and reaches the core only when the fuse permits; the DM
      control state is TMR, the TAP transport deliberately is not
  - id: Z3-R15
    text: The imported SoC cluster (SERV core, mask ROM with firmware,
      single-master bus with watchdog, UART) shall boot the committed
      firmware and answer on its pins - proving the ZIRH-2 verification
      methodology carries to this tree unchanged before it is wired to
      the memory subsystem.
    tests:
      - test/test_soc.py
    evidence: the SoC cocotb suite retargeted, 3/3 (boot, echo through
      the pins, the living-CPU contract); import ladder rung 2
  - id: Z3-R16
    text: The CPU-carrying top shall load a CRC32-sealed image over the
      UART pin through the loader into the ECC-protected bank while the
      cluster is held in reset, release the CPU only after read-back
      verification, run the LOADED program observably (its output on
      the TX pin), refuse corrupt images with a fallback to the golden
      ROM, and behave identically to the standalone cluster when
      strapped golden.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: rung-3 suite 3/3 - the loaded program's byte stream
      audible on the pin, corrupt-CRC fallback proven by the ROM's
      echo, golden-strap regression; 45 replicas / 2984 flops measured
      on the full top
  - id: Z3-R17
    text: The five-macro sliced SECDED bank shall serve as CPU data
      memory on the system bus - a loaded program shall write it, read
      it back through the corrected port, and the round trip shall be
      observable from outside the die.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: the rung-4 bank-prober - loaded code writes 0x4000,
      reads back corrected, and shouts the verdict byte on the TX pin;
      full-top synthesis integrity at 51 replicas / 3253 flops
  - id: Z3-R18
    text: The debugger's System Bus Access master shall read and write
      system memory (the sliced bank) WITHOUT halting the core, taking
      priority over the CPU on the shared port, and every such access
      shall pass through the flight-locked gate: live on the bench,
      wholly inert in flight.
    tests:
      - test/tb_sba.v
      - scripts/check_tmr.sh
    evidence: the SBA smoke - unlocked, a JTAG debugger pokes 0xBEEF
      into the bank and reads it back; locked, the gate holds the SBA
      inert and a seeded word survives an attempted overwrite
  - id: Z3-R19
    text: The compute die shall carry the housekeeping/telemetry cluster
      - the SEU counters, the CPU signature, and the watchdog - and the
      watchdog-revert signon shall complete the boot contract: a loaded
      bank that signs on runs, a loaded bank that never signs on is
      failed by the watchdog after a grace period and reverted to the
      golden ROM, and unprompted telemetry frames carry a valid
      checksum and a living, changing CPU signature.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: rungs 3-6 suite 6/6 - telemetry frames carry a changing
      CPU signature, and a silent loaded bank is reverted to ROM by the
      watchdog whose echo then answers; full die at 70 replicas / 4192
      flops
  - id: Z3-R20
    text: The sliced SECDED bank's march-test engine shall be reachable
      from software through a bus doorway - a manufacturing-test program
      loaded over the ISP shall start a march, poll the busy bit, and
      report the verdict on the UART, and an in-flight self-test shall
      require nothing more than the same firmware loop.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: slot-5 MBIST doorway - an ISP-loaded branchless runner
      starts march c-, outlasts the busy stream, and the 0xB1 pass
      verdict reaches the pin; the doorway's mode register holds its
      replicas (3/8 measured)
  - id: Z3-R21
    text: The TAP shall implement SAMPLE/PRELOAD and EXTEST over a
      boundary register spanning every functional pin, and EXTEST's
      pin-drive effect shall be masked by the same absorbing flight
      lock that guards halt and system bus access - board-level
      interconnect test on the bench, an untouchable pin ring in
      flight.
    tests:
      - test/tb_bscan.v
    evidence: SAMPLE reads the real pins through the TAP, EXTEST drags
      uart_tx low and boot_sel high from the chain when unlocked, and
      the same preload lands on deaf pins when locked
  - id: Z3-R22
    text: The dedicated die shall have a proven post-synthesis path to
      full scan that leaves the verified RTL untouched - synthesis to
      foundry cells, flop-for-scan-flop substitution, deterministic
      chain stitching, and a simulated proof of shift and capture on
      the stitched netlist.
    tests:
      - scripts/dft_scan.sh
    evidence: pilot block zirh_clkobs - all 48 flops land in one chain
      on real SG13G2 scan cells, the pattern shifts through intact,
      and a capture cycle overwrites the loaded ones with the true
      reset state (the three reset-to-one flops read back as the only
      ones)
  - id: Z3-R23
    text: The compute upgrade shall have a proven protection path. Two
      candidates are demonstrated on real cells and measured - triple-core
      lockstep (voted bus, sticky divergence flag, recovery via the
      existing reset ladder) and tool-applied flop triplication with
      voted feedback (per-flop self-heal, RTL untouched) - and the
      50 MHz closure campaign selects between them on measured timing.
    tests:
      - test/tb_vex_tmr.v
      - scripts/tmr_pilot.sh
    evidence: pilot A - a corrupted core is outvoted for 2000 cycles
      and flagged sticky; pilot B - the stitched netlist tracks the
      golden model, masks a pinned replica for 100 cycles, and heals
      it on release; core transform measured at 3.5x cells with the
      cache-array caveat recorded
  - id: Z3-R24
    text: The die shall carry at least 64 KB of protected data storage
      built from the proven sliced-SECDED bank - the CPU reaching it
      through a paged window that leaves the bus map and the golden ROM
      untouched, the debugger reaching it flat, and every page carrying
      its own scrubber and march engine behind the doorway.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: sixteen pages of zirh_sram39 (80 macros) behind the
      TMR'd page register; an ISP-loaded program proves two pages hold
      different words at the same window address and reads both back;
      the full-size array measures 73 replicas / 31406 flops in the
      guard
  - id: Z3-R25
    text: The macro-bound shipping core shall close 50 MHz setup timing
      through full place-and-route on SG13G2 with the cache arrays on
      real RM 2P macros, and any residual hold deficit shall be named,
      bounded, and assigned to signoff.
    tests:
      - pnr/vexlitemac/config.json
    evidence: campaign record in the config and ROADMAP - setup ws
      +5.44 ns at the slow corner post-route, zero routing overflow,
      routing DRC clean; the signoff ECO (Cycle 16) then closed the
      hold residual - 14 buffers on 9 endpoints, 3-corner SPEF
      session reading -0.39 before and +0.15 after with setup
      untouched: every corner closes at 50 MHz
  - id: Z3-R26
    text: The die shall run loaded programs FROM the protected paged
      bank - the loader writing the image flat into the same array the
      scrubbers guard, the CPU fetching flat through its cache behind a
      four-master arbiter (debug, loader, data, fetch), and the golden
      ROM fallback, watchdog revert and MBIST contracts surviving the
      move unchanged.
    tests:
      - test/test_top_isp.py
      - test/tb_sba.v
      - scripts/check_tmr.sh
    evidence: top suite 8/8 on the program-store architecture at first
      run - loaded code executes from the bank via flat fetch, corrupt
      images still fall to ROM, the watchdog still reverts, MBIST
      marches a non-code page, paging isolation holds; boot/soc/SBA/
      boundary-scan/SV suites green; guard at 73/31427 with the
      loader's widened index replicas measured
  - id: Z3-R27
    text: The bank shall scale to 256 KB on the same eighty macros by
      deepening the slices to 4096 rows - banks A/B at 128 KB each, the
      CPU window paged in six bits (instance plus sub-page), the
      address-in-ECC fold bit-identical at the proven default depth,
      and the march unit becoming the 16 KB instance with the test
      runner keeping its computer alive while it tests.
    tests:
      - test/test_top_isp.py
      - scripts/check_tmr.sh
    evidence: DEPTH parameter selects the RM 4096x8 slice; top suite
      8/8 at the 256 KB geometry including the paging isolation and
      the watchdog-petting MBIST runner on instance 1; guard at
      76/31761 with the wider page and scrubber replicas measured
  - id: Z3-R28
    text: The ECO'd layout shall be confirmed by a full routing pass
      with real extracted parasitics at three corners, and the confirm
      shall be a self-healing signoff loop - when the routed truth
      exposes an undershot repair, the repair is redone against that
      truth until every corner closes.
    tests:
      - pnr/vexlitemac/eco_confirm.tcl
    evidence: run 32186904473 - pass 1 measured the estimate's flattery
      (hold -0.53 routed vs +0.15 estimated), the loop inserted two
      more hold buffers against the extracted SPEF, and pass 2 closed
      hold +0.16 / setup +4.71 at every corner on the exact layout
  - id: Z3-R29
    text: The tool-stitched TMR netlist of the compute cluster shall
      boot the full ISP story at gate level; a wound that kills one
      replica rail outright shall be masked by the vote and healed on
      release; a majority wound shall provably kill the machine (the
      control that keeps the proof honest); and the proof-of-life
      discipline shall be immune to the free-running telemetry stream.
    tests:
      - scripts/gl_boot.sh
      - test/test_top_gl.py
    evidence: 2138 flops stitched and structurally checked, none left
      behind (coverage guard); the flood discipline exposed the naive
      single-0x5A hunt as telemetry-contaminated and then exposed a
      fresh-bit collision in the stitcher itself (netname bits missing
      from the seed - raw replicas aliased onto stale nets); with both
      fixed, the rail-B wound booted, healed 2138/2138 and kept
      speaking, and the majority-wound control fell silent
  - id: Z3-R30
    text: The tool-stitched TMR netlist shall close 50 MHz through
      full place-and-route on real silicon geometry - synthesis
      disabled so the optimizer cannot collapse the triples, timing
      closed at every corner on the routed and extracted layout, and
      every replica rail counted BY NAME in the routed database.
    tests:
      - pnr/vextmr/config.json
      - pnr/vextmr/confirm.tcl
    evidence: run 32295970007 - hold +0.19 / setup +2.58 at every
      corner after the two-arm signoff loop (hold and setup repaired
      against the routed truth); 2138 tmrA + 2138 tmrB + 2138 tmrC
      rail nets counted in the routed DEF at both gates; 6414 flops
      in the DB, the DEF and the flow metrics; protection price
      measured 2.49x cell area at unchanged density
  - id: Z3-R31
    text: The netlist extracted from the CONFIRMED layout database
      itself - clock tree, repair buffers and tie cells included -
      shall boot the full ISP story and survive the wound trio under
      the same benches as the pre-layout proof, with every replica
      rail present by name in the netlist text.
    tests:
      - pnr/vextmr/dump_netlist.tcl
      - scripts/gl_pnr_boot.sh
    evidence: run 32330298237 - the confirmed database's own verilog
      passes clean flood, rail-B wound with 2138/2138 heal and the
      majority-wound control; 2138 tmrA/B/C rails counted in the
      netlist text before simulation; the flow netlist passed the
      same trio locally first
  - id: Z3-R32
    text: The physical-signoff legs shall run on the stitched layout
      and be gated - the two GDS streamouts shall merge the macro GDS
      to identical geometry, and LVS shall match every device exactly,
      with any mismatch pinned to an enumerated, root-caused tool-view
      class so that a new class fails the gate.
    tests:
      - scripts/xor_streams.py
      - scripts/signoff_lvs.sh
    evidence: runs 32385949265 and 32389012022 - XOR zero shapes
      across 38 layers between the magic and klayout streams; netgen
      devices 36232 both sides with both RM macros matched one to
      one; the pinned ledger holds exactly six macro power-pin nets
      (LEF vs verilog naming) and four constant-bit port symmetries,
      nothing else
  - id: Z3-R33
    text: The stitched core shall complete the boot story under a rain
      of random single-replica upsets - each shot one clock long,
      verified to land and verified to heal - with the image committed
      and the program flood alive at the end, and the rain dense
      enough that a thin storm fails the gate.
    tests:
      - test/test_top_seu.py
      - scripts/gl_boot.sh
    evidence: three seeds locally, 314 shots total (106 + 102 + 106,
      rails drawn uniformly), 314 healed, zero divergence; the first
      storm failed its own thinness gate (53 of 66 slots lost to
      unwritten X flops) and the redraw fix is recorded; seed 1 runs
      in CI as stage 7 of the gl ladder
  - id: Z3-R34
    text: The WHOLE die - loader, bank arbiter, telemetry, watchdog,
      debug gate, POR - shall boot the ISP story as SG13G2 gates with
      the stitched CPU linked in its socket, and the wound trio and
      the SEU rain shall hold on that full gate die under the same
      benches, unchanged.
    tests:
      - scripts/gl_die.sh
      - test/Makefile.gltop
    evidence: the die synthesizes to 41848 top-level cells with its
      hierarchy preserved (so u_soc.u_cpu keeps its path and every
      wound bench runs unmodified); sim parameters and the ROM hex
      are baked with chparam; the gate die boots the flood in 43 s,
      the wound trio passes 3/3 and the rain heals 106 of 106 - the
      gldie ladder runs on every push
  - id: Z3-R35
    text: The design shall survive seeded uncurated storms at two
      boundaries - the loader fuzzed at its own ports and the whole
      die fuzzed at the pins - under continuous invariants, a liveness
      cadence no random history may break, and coverage closure gates
      that fail a storm too thin to prove anything.
    tests:
      - test/sv/zirh_boot_fuzz_tb.sv
      - test/test_top_fuzz.py
    evidence: SV storm 3 seeds x 120 episodes, 693 checks, invariants
      silent, deterministic per seed; top storm 3 seeds x 10 blocks
      with every armed and closed bin filled. The storms earned their
      keep before passing - the first drafts exposed the loader's
      unstated one-ruling-per-reset contract (S_RUN listens only at
      strap 11; S_GOLDEN is deaf) and the bench-side mid-stream
      alignment trap hunt_echo's docstring had predicted, both now
      locked in oracles
  - id: Z3-R36
    text: The die shall carry 56 GPIO at the yardstick's count and
      split - PORTA 32 and PORTB 24 - with TMR'd output and direction
      registers feeding the error aggregate, double-synchronized
      inputs, and an ISP-loaded program proven to own both directions
      of the pin boundary.
    tests:
      - test/test_gpio.py
      - test/test_top_gpio.py
    evidence: block suite proves registers, masks, sync latency and
      the once-per-bus-write load; the through-the-die suite loads a
      program that drives PORTA, opens all 32 drivers, reads the
      bench's PORTB pattern through the synchronizers and floods its
      verdict; guard measured 6 replicas / 453 flops for the block,
      79 / 32214 for the top; the gate die re-synthesizes and boots
      with the block in
  - id: Z3-R37
    text: The stitched netlist shall be FORMALLY equivalent to what it
      was stitched from - the full RTL-to-stitched chain proven in one
      equivalence at pilot scale, and the stitch transformation proven
      at full core scale by induction over every flop boundary, with
      the macros cut to the boundary and an anti-vacuity gate on the
      proven point count.
    tests:
      - scripts/formal_equiv.sh
      - scripts/formal_eqlib.py
      - scripts/formal_eqprep.py
    evidence: pilot leg 16 of 16 points proven (RTL LFSR vs its
      synthesized-and-stitched netlist, one proof); core leg 2639 of
      2639 points proven (311 combinational, 2328 by induction) in
      131 seconds - cell functions generated from the PDK liberty,
      match points from the stitcher's own bookkeeping, RM macros as
      boundary ports; both legs gated against vacuous passes
  - id: Z3-R38
    text: Place-and-route shall be proven function-preserving on the
      stitched core - the netlist extracted from the confirmed layout
      formally equivalent to the stitched netlist that entered the
      flow, matched on the rail names both carry, macros cut to the
      boundary, the clock tree and repair buffers collapsed by the
      proof itself, and the padframe power budget shall be written
      from measured currents, not habit.
    tests:
      - scripts/formal_equiv.sh
      - scripts/formal_eqprep.py
    evidence: 6915 of 6915 points proven (283 combinational, 6632 by
      induction) in 408 seconds against the Cycle 21 boot artifact's
      netlist; the unread A_DOUT macro pin taught the cut to skip
      dangling outputs on both sides; SCOPE.md carries the pad budget
      - 4 core pairs + 1 array pair + 7 SSO-ruled VDDIO/VSSIO pairs,
      about 12 ground pads against 74 signals at a 1:6 ratio
  - id: Z3-R39
    text: The die shall carry 24 32-bit timers at the yardstick's
      count with timer, PWM, capture and pulse modes, pinned through
      PORTB alternate functions at zero pad cost, every register
      TMR'd including the running counters, and timer 0's overflow
      wired to the core's timer interrupt.
    tests:
      - test/test_timer.py
      - test/test_top_timer.py
    evidence: block suite proves wrap/flag/W1C, 30 percent duty on
      the pin, capture latching an earlier count, five edges counted
      as five, ALTB ownership and the irq raise-and-drop; the
      through-the-die suite ISP-loads a program that leases pin 5 to
      timer 5 and the bench measures the duty on the pin; guard
      measured 12 replicas / 9938 flops for the bank, top at 42104
      flops; the gate die re-synthesizes and boots with all 24 in
  - id: Z3-R40
    text: The die shall carry two I2C masters at the yardstick's
      count - byte-command engines with open-drain discipline on
      PORTA alternate functions, obeying a stretching slave, every
      register TMR'd including the engine state - reachable at slot
      6's sub-decoded upper half.
    tests:
      - test/test_i2c.py
      - test/test_top_i2c.py
    evidence: block suite proves START/byte/ACK, data plus STOP, a
      read leg served and NACKed, and a stretched clock obeyed with
      the byte landing intact; the through-the-die suite ISP-loads a
      program that commands I2C0 and the bench slave on the PORTA
      pins sees exactly the START and the byte; guard measured 21
      replicas / 214 flops per master, top at 42524 flops; subset
      recorded: master only, no slave mode
  - id: Z3-R41
    text: The die shall carry three SPI masters at the yardstick's
      count - full-duplex 8-bit engines with all four CPOL/CPHA modes
      and software-owned chip select on PORTA alternate functions,
      every register TMR'd - reachable at slot 7's sub-decoded upper
      half.
    tests:
      - test/test_spi.py
      - test/test_top_spi.py
    evidence: block suite proves mode-0 full duplex both directions
      across two bytes of one CS frame, the CS pin under software,
      and mode 3 idling high and transferring both ways; the
      through-the-die suite ISP-loads a program that clocks a byte at
      the bench slave and floods its verdict on the readback; guard
      measured 12 replicas / 164 flops per master, top at 43010
      flops; subset recorded: master only, 8-bit words
  - id: Z3-R42
    text: The die shall carry a second UART - a programmable 8N1
      endpoint with exact-divisor bit periods, half-bit-centered
      sampling, false-start rejection and read-clears-valid flow, all
      state TMR'd, on PORTA alternate functions - closing the
      yardstick's interface ledger.
    tests:
      - test/test_uart1.py
      - test/test_top_uart1.py
    evidence: block suite proves the frame on the pin at the
      programmed rate, an injected frame landing with valid, the
      read-clear, glitch rejection and newest-byte overwrite; the
      through-the-die suite runs two UARTs at two rates in one
      program - the bench byte received on UART1, echoed on UART1,
      verdict flooded on UART0; guard measured 12 replicas / 272
      flops, top at 43280; the exact-div lesson recorded: a +1
      period convention drifts one clock per bit
  - id: Z3-R43
    text: The die shall carry an interrupt fabric - thirty-two level
      sources (24 gated timer overflows, UART1 rx/tx, both I2C and
      all three SPI ready lines) behind one TMR'd mask with readable
      RAW and PENDING words at slot 6, the masked lines driving the
      core's external interrupt array - and the core's timer
      interrupt line shall be connected, not tied.
    tests:
      - test/test_irq.py
      - test/test_top_irq.py
    evidence: block suite proves reset-masked silence, PENDING as
      exactly RAW AND MASK, the no-latch law (a source cleared at its
      peripheral drops from PENDING the same cycle) and once-per-
      strobe mask writes; the through-the-die suite ISP-loads a
      program that unmasks timer 0, arms a five-cycle period and
      POLLS PENDING before flooding its verdict, then the bench peeks
      the core boundary - bit 0 standing, all masked sources silent;
      the integration surfaced and closed a latent defect: the soc
      had tied the core's timer_irq input to zero since Cycle 30;
      guard measured 3 replicas / 98 flops, top at 43414
  - id: Z3-R44
    text: The payload UART shall carry the yardstick's depth - a
      sixteen-deep TMR'd FIFO on each direction built from a
      reusable queue primitive, programmable parity and a second
      stop bit, and sticky write-1-to-clear flags for overrun,
      framing and parity faults - with 8N1 defaults bit-identical
      to the Cycle 33 frame.
    tests:
      - test/test_fifo.py
      - test/test_uart1.py
      - test/test_top_uart1.py
    evidence: the primitive's suite proves order through fill and
      drain, refusal at both walls, an exact level gauge against a
      running model across many wraparounds; the UART suite proves
      in-order queueing both directions, the overflow law (the
      seventeenth byte is dropped, OE goes sticky, the oldest
      sixteen survive), parity emitted and enforced (a lying frame
      sets PERR and never enters the queue), FERR on a broken stop,
      and stop2 stretching the frame by exactly one bit time; the
      old newest-byte-overwrite law is repealed and the repeal is
      tested; guard measured 432 flops for the primitive, 15
      replicas / 1137 flops for the UART, top at 44279
  - id: Z3-R45
    text: The SPI trio shall carry the yardstick's width and depth -
      word lengths of 4 to 16 bits with WLEN resetting to 8, a
      sixteen-deep TMR'd FIFO on each direction so bursts fly under
      one held chip select, an OE flag for the reply that met a full
      queue, and four decoded chip-select lines steered by CSSEL,
      software-owned as ever, reaching pins only under MCS.
    tests:
      - test/test_spi.py
      - test/test_top_spi.py
    evidence: the block suite proves 4, 12 and 16 bit words full
      duplex both directions, a three-word burst draining in order
      under one select, the seventeenth unread reply lost with OE
      sticky and W1C, and CSSEL lowering exactly the chosen line
      only under MCS; the through-the-die suite adds a 12-bit word
      through the pin boundary with the select riding a leased
      extra pin while the classic line stays high; the phase lesson
      recorded - the final edge of a CPHA-1 word is itself a
      sampling edge, so the queue takes the register's NEXT value;
      the review round then hardened the laws: the receive mask is
      LATCHED at launch so a WLEN write cannot truncate the word in
      flight (proven by reconfiguring mid-transfer), both phases now
      run at every width, WLEN readback and its reset default are
      read, the overflow proof uses distinct replies and pops the
      survivors, and the DIV >= 3 read law is recorded - a two-flop
      listener needs a half-bit longer than its own lag; guard
      measured 15 replicas / 1902 flops, top at 49493
  - id: Z3-R46
    text: Each I2C controller shall carry the yardstick's other
      chair - a slave engine behind a seven-bit address, with
      sixteen-deep TMR'd queues both directions, protocol-native
      backpressure (a full receive queue NACKs the byte on the
      wire, losing nothing), all-ones service with a sticky UE flag
      when read empty, repeated-START honored, and the master
      chair parked while the slave sits.
    tests:
      - test/test_i2c.py
      - test/test_top_i2c.py
    evidence: the block suite proves silence at a stranger's
      address, three bytes queued in arrival order, reads served
      from the preloaded queue with the master's NACK ending the
      transaction, the sixteen-byte wall answered with a NACK and
      zero loss, underflow all-ones with UE write-1-to-clear,
      repeated-START switching direction without a STOP, and the
      parked master refusing a leg; the through-the-die suite has
      the BENCH as the bus master against the die's own pins - the
      die answers its name, queues the master's byte for software
      and serves the preload back; guard measured 24 replicas /
      1168 flops, top at 51401
